# spec-delta：install-lifecycle

> 对 `openspec/specs/install-lifecycle/spec.md` 的修改。

- ~ Codex runtime 探测目录从 `~/.codex` 改为 `~/.agents`。
- ~ `(codex, user)` 目标目录从 `~/.codex/skills/` 改为 `~/.agents/skills/`。
- ~ `(codex, project)` 目标目录从 `<git-root>/.codex/skills/` 改为 `<git-root>/.agents/skills/`。
- ~ Claude Code 与 Hermes 的 runtime、scope 和路径行为保持不变。
