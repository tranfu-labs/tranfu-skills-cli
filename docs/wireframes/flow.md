# 页面流转图

TODO: 需人工确认 —— 本项目是 CLI 工具（`tfs`），无 web 路由 / 无图形界面。

按 init 阶段的默认规则，`docs/wireframes/` 静态骨架仍铺好（含 `AGENTS.md` / `CLAUDE.md` / `legend.md` / `_template/page.md`），但本文件没有真实"页面流转"可画。是否保留整个 `docs/wireframes/` 目录，按根 [AGENTS.md](../../AGENTS.md) 的「线框图」一节的删除规则决定（确认无界面后可整目录 rm + 删除该节）。
